descriptor:
  type: rdc
  params:
    r_min: 0.0
    r_max: 6.0
    dr: 0.05
    alpha: 10.0

model:
  hidden_layer_sizes:
    - 100
  activation: relu
  solver: adam
  alpha: 0.0001
  batch_size: auto
  learning_rate: constant
  learning_rate_init: 0.001
  max_iter: 200